.input-row.fade-in(ng-repeat='metaInput in doc.metaInputs | filter:showInput track by metaInput.id', ng-include='getInputTemplateName(metaInput)', ng-init='inputsInner = inputs; metaInputs=doc.metaInputs')
